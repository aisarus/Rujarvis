# Пустое основание для разбора всей кодовой базы

Эта ветка нужна ровно для одного: CodeRabbit смотрит только diff, а чтобы он
увидел весь проект, PR должен идти из `main` в пустоту. Тогда diff равен
всему коду.

Ветка не для слияния. PR, открытый от неё, закрывается после разбора.
